//function is a set of statements which will do some specific work

function fun1(a,b)
{
    var res = a+b;
    return res;
}
console.log(fun1(10,30));

//Anonymous Function
//Function Expression is similar ti abs has the same syb=ntax
//as a function decleration one can define "named" function expression(where the name of the expression might be used in the call stack for example or anonymous function expression)


var funcExp = function(a,b)
{
    return total = a+b;
}

console.log(funcExp(10,50));